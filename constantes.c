//Se vc não quer outros, ou vc msm, mudando valores de variaveis existentes, vc pode usar o codigo "const", assim vc não consegue mudar essa variavel dps
#include <stdio.h>

int main(){
    const int num1 = 10;
    printf("O valor da cosntante num1 é: %d\n", num1); 
}